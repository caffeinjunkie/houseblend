/**
 * Check value for null
 * @param  {any} value
 * @returns {boolean} true if value is null, false if value is not null
 */
const isNull = (value) => {
  return value === null;
};

module.exports = {
  isNull
}